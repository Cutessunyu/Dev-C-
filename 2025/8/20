#include<stdio.h>
#include<iostream>
#include<string>
using namespace std;

struct book{
	string s;
	int g, u;
};

int main()
{
	int n;
	scanf("%d",&n);
	 book b[1001];
	for(int i=0;i<n;i++)
	{
		cin>>b[i].s;
		scanf("%d %d",&b[i].g,&b[i].u);
	}
	string w;
	cin>>w;
	for(int i=0;i<n;i++)
	{
		if(w==b[i].s)
		{
			printf("%d %d",b[i].g,b[i].u);
			break;
		}
	}
}


#include<stdio.h>
#include<stack>
#include<queue>
using namespace std;

int main()
{
	int a;
	stack<int> s;
	queue<int> q;
	for(int i=0;i<10;i++){
		scanf("%d",&a);
		s.push(a);
		q.push(a);
	}
	
	for(int i=0;i<10;i++){
		printf("%d ",s.top());
		s.pop();
	}
	printf("\n");
	for(int i=0;i<10;i++){
		printf("%d ",q.front());
		q.pop();
	}
}


#include<stdio.h>
#include<string>
#include<iostream>
#include<stack>
#include<queue>
using namespace std;

int main()
{
	
	int n,a;
	stack<int> s;
	queue<string> q;
	scanf("%d", &n);
	for(int i=0;i<n;i++)
	{
		string b;
		scanf("%d",&a);
		cin >> b;
		s.push(a);
		q.push(b);
		 
	}
	while(!s.empty())
	{
		printf("%d ",s.top());
		s.pop();
	}
	printf("\n");
	while(!q.empty())
	{
		cout << q.front();
		printf(" ");
		q.pop();
	}
	
}



#include<stdio.h>
#include<stack>
using namespace std;

int main()
{
	stack<int> s;
	int n,s,a;
	scanf("%d",&n);
	for(int i=0;i<n;i++)
	{
		scanf("%d",&a);
		while(!s.empty()&&s.top<a)
		{
			s.pop();
		}
			s.push(a);
	}
	printf("%d",s.size());
}
